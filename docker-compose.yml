services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: incident_responder
      POSTGRES_PASSWORD: incident_responder
      POSTGRES_DB: incident_responder
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
